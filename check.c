#include<stdio.h>

int main(){
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    int first,second,third;
    if(a>=b && a>=c) first = a;
    if(b>=a && b>=c) first = b;
    if(c>=a && c>=b) first = c;
    if(a>=b && a>=c) third = a;
    if(a>=b && a>=c) third = a;
    if(a>=b && a>=c) third = a;

}